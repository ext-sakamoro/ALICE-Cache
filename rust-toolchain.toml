# Rust toolchain pin (feedback_clippy_rust_version_drift 準拠)
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
# no_std 検証用 bare-metal target (64-bit atomics 必須なので aarch64、pinned toolchain 側に install)
targets = ["aarch64-unknown-none"]
profile = "minimal"
